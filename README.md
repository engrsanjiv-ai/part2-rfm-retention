# Part 2: RFM Segmentation & Retention Strategy

## D2C Customer Churn Intelligence — Capstone Project

### Overview
This repository builds a multi-signal customer segmentation system using RFM (Recency, Frequency, Monetary) scores enriched with behavioural, support, and campaign signals. Each segment receives a tailored retention strategy. A manual review section covers 10+ edge-case customers where the retention decision is non-obvious.

---

## Repository Structure

```
part2-rfm-retention/
├── README.md
├── rfm_segmentation.ipynb         # Full segmentation notebook
├── segments.csv                   # Customer → segment mapping with features
├── retention_strategy.md          # Per-segment retention playbook + budget plan
├── manual_review_cases.md         # 10 ambiguous customer cases with reasoning
├── requirements.txt
├── charts/                        # Generated visualizations
└── outputs/                       # Intermediate files generated by notebook
    ├── edge_cases.csv
    └── segment_summary.csv
```

---

## Dataset

Download from:
[Google Drive Dataset Package](https://drive.google.com/drive/folders/1PmLapJI1VSDgvl_AxARNKwM1MCd3WFX0?usp=sharing)

Place all CSV files in a `data/` folder at the project root:

```
part2-rfm-retention/
└── data/
    ├── customers.csv
    ├── orders.csv
    ├── support_tickets.csv
    ├── web_events_snapshot.csv
    ├── churn_labels.csv
    ├── intervention_history.csv
    └── rfm_modeling_snapshot.csv
```

---

## Setup Instructions

**1. Clone & Create Virtual Environment**
```bash
git clone https://github.com/engrsanjiv-ai/part2-rfm-retention.git
cd part2-rfm-retention
python -m venv venv
```

**2. Activate Virtual Environment**
- **Windows**: `venv\Scripts\activate`
- **Mac/Linux**: `source venv/bin/activate`

**3. Install Dependencies & Run**
```bash
pip install -r requirements.txt
jupyter notebook rfm_segmentation.ipynb
```

Run all cells from top to bottom. `segments.csv` and all outputs are saved automatically.

---

## Segments Defined

| Segment | Logic Summary |
|---------|--------------|
| Champions | High R + High F + High M |
| Loyal Customers | High F + High M, moderate R |
| Promising | Recent first/second order, moderate M |
| At-Risk High Value | Previously high M/F, now dropping recency |
| Discount Seekers | High F but low M, high discount usage |
| High Value Unhappy | High M but high ticket count / low CSAT |
| Dormant | Low R (60+ days), low F |
| New Customers | Joined recently, only 1–2 orders |

---

## Key Outputs

| File | Description |
|------|-------------|
| `segments.csv` | customer_id, segment, R/F/M scores, enrichment signals |
| `outputs/segment_summary.csv` | Per-segment summary metrics for analysis |
| `outputs/edge_cases.csv` | Edge-case customers for manual review |
| `retention_strategy.md` | Full strategy + budget prioritization |
| `manual_review_cases.md` | 10 edge-case customers with reasoning |

---

## Notes
- All paths are relative — no hardcoded local paths
- Snapshot date used for recency calculation to prevent leakage
- RFM scores are quintile-ranked (1–5 scale)
- Python 3.9 recommended
